# Optional static form endpoint used by ContactForm. Leave blank to show production-safe direct contact CTAs.
# Examples: a Formspree endpoint, Basin endpoint, or a Cloudflare Pages Function route such as /api/contact.
PUBLIC_FORM_ENDPOINT=
